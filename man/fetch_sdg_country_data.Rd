% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_sdg_country_data.R
\name{fetch_sdg_country_data}
\alias{fetch_sdg_country_data}
\title{Fetch Country-Level SDG Data from UN SDG API}
\usage{
fetch_sdg_country_data(
  goal = NULL,
  indicator = NULL,
  country = NULL,
  year_range = NULL,
  save = FALSE,
  save_path = "./data/sdg_downloads/",
  formats = c("csv", "rds")
)
}
\arguments{
\item{goal}{Numeric (1-17). Optional SDG goal number.}

\item{indicator}{Character. SDG indicator code (e.g., "15.3.1").}

\item{country}{Character. ISO3 country code (e.g., "IND", "AUS").}

\item{year_range}{Numeric vector of length 2 (start_year, end_year).}

\item{save}{Logical. If TRUE, saves data to disk. Default FALSE.}

\item{save_path}{Character. Directory path for saving data.
Default: "./data/sdg_downloads/".}

\item{formats}{Character vector. Any combination of "csv", "rds".
Default c("csv", "rds").}
}
\value{
A data.table containing SDG indicator data.
}
\description{
Retrieves Sustainable Development Goal (SDG) indicator data from the
official United Nations SDG API.
}
\details{
At least one of \code{goal} or \code{indicator} must be provided.
\itemize{
\item If \code{indicator} is supplied → fetch that specific indicator.
\item If only \code{goal} is supplied → fetch all indicators under that goal.
\item If both are supplied → consistency is validated.
}

\code{country} and \code{year_range} act as filters.
}
